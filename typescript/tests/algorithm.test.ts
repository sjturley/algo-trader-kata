import { RefactorMeAlgorithm } from '../src/refactor_me_algorithm';

describe('algorithm', () => {
  it('executes trades', () => {
    const refactorAlgorithm = new RefactorMeAlgorithm();
    refactorAlgorithm.run();

    // TODO assert something
  });
});
